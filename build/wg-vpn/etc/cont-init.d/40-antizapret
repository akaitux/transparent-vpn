#!/bin/bash
##!/command/with-contenv bash

if [ ! -f /opt/antizapret/config/config.sh ]; then
    echo "#### Init antizapret ####"
    mkdir -p /opt/antizapret/config
    cp /opt/antizapret/config_default/* /opt/antizapret/config/
fi
nohup /opt/antizapret/doall.sh 2>&1 >/dev/null &
